# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.3.12] - 2026-09-23

### 修复
- **黑色星号仍在（彻底解决）**：上一版只擦掉了星号的橙红填充，
  AI 给它加的**黑色描边**完整保留 → 屏幕上依然是一个黑色星号（用户截图证实）。
  - 失败尝试记录：改用「连通域」擦除时，星号与人物头发被连成一片，
    把**人物右侧头发一起删掉了**（ASCII 差异图发现，见
    `tool/diff_erase.dart`）——随即放弃该方案。
  - **最终方案：从源头解决**。重新生成素材，提示词明确要求
    「画面上没有任何符号、没有文字、没有装饰图案」并加入负面词
    （星号/十字/怒气符号/emoji/图标…），VLM 质检确认两张候选**均无符号**
    （9 分），选用构图居中那张。💢 现在完全由代码绘制，素材零残留。
  - 附带好处：不再需要任何像素级擦除，也就没有误伤人物的风险。

### 工具
- 新增 `tool/inspect_anger_region.dart`：把指定区域打成 ASCII 图，
  用像素级证据确认"到底残留了什么"（而不是靠肉眼猜）。
- 新增 `tool/diff_erase.dart`：擦除前后轮廓对比（发现误伤头发就是靠它）。
- 新增 `tool/vlm_check_clean.py`：**"无符号"专项质检**——直接问
  VLM 画面里有没有星号/十字/emoji 等装饰，从素材入库前就拦掉问题。

### 测试
- 素材回归测试升级：改为**全图连通域扫描**——主人物之外若存在
  超过 200px 的独立块，即判定"残留符号"并报错。
  （旧的"只看橙色像素"测试正因为漏掉黑色描边而放过了一个缺陷版本。）
- 累计 **189 项全部通过**。

## [1.3.11] - 2026-09-23

### 修复
- **💢 生气十字画歪（黑色六角星）**（回归）：上一版改用代码绘制时用了
  `quadraticBezierTo` 手写十字轮廓，控制点算错画成了黑色六角星。
  - 修复：改用「两条矩形 + 臂端圆帽圆」最简单的图元拼十字——
    形状 100% 可控，绝不会画歪；颜色统一经典动漫怒红（无阴影层覆盖）。
  - 教训：小符号不需要复杂 Path，基础图元拼出来的形状才是确定的。

### 测试
- 累计 **189 项全部通过**。

## [1.3.10] - 2026-09-23

### 修复
- **引导把用户操作误判为"不听话"（严重）**：用户点了放大镜、也进了搜索页，
  引导却仍停在"点放大镜"，后续点击全被当成点错地方——越点语气越差，
  甚至可能触发锁屏（用户截图证实）。
  - 根因①（判定时序）：锚点在 **build 时缓存**"我是不是当前目标"。
    引导从开场推进到「点放大镜」时锚点组件不在引导层内、不会重建，缓存仍为
    false → 点击被**漏判**（不推进），但放大镜自己生效（跳页）→ 引导卡死。
    修复：判定挪到**点击当下**实时读取（`onTargetTapped`），杜绝缓存过期。
  - 根因②（目标消失）：引导指着某控件时用户若用**系统返回键**关掉页面
    （而非点引导指向的返回箭头），目标随页面销毁，引导卡在消失的目标上。
    修复：目标销毁即视为该步完成（`onAnchorGone`），返回键也是返回。
  - 根因③（气泡吞点击）："点一下继续"的对话框被点击时被组件吞掉却无反应。
    修复：气泡不吃手势，点击穿透到统一逻辑（该步无目标 → 直接继续）。
- **"功能符合直觉"整体调优**：
  - 新增 `freeRoam`（自由参观）步骤：搜索页这步**不铺遮罩、不计不听话**——
    用户想先搜本书再返回完全合理，引导不该把页面堵死；
  - 目标暂不可见时引导**让路**（不再用全屏遮罩堵一个看不见的目标）；
  - 面板入口（＋）点击后立即让路，面板关闭自动继续（原 `waitNote` 机制简化）。

### 测试
- 新增「直觉交互」回归组 4 项：点中目标立即推进 / 系统返回键关页视为完成 /
  自由参观不计劝导 / 面板开合自动接续。
- 累计 **189 项全部通过**。

## [1.3.9] - 2026-09-23

### 修复
- **生气立绘右上角的"红色星号"**：AI 画的「怒气符号」画歪成了六角星，
  挂在头上很难看（用户反馈"红色星号"）。
  - **素材**：用像素聚类定位右上角橙红簇（2563px），连同描边一并擦除，
    人物腮红/唇色小簇不受影响；原图已备份到 `pet_rage_bak.png`。
  - **渲染**：💢 改为**代码精确绘制**的四臂十字（端头圆帽 + 暗红投影），
    位置对准原星号坐标（头部右上），保证形状与位置永远正确。
    AI 画符号这类「小装饰」容易画歪，代码绘制才是稳的。

### 工具
- 新增 `tool/erase_anger_star.dart`：像素聚类定位并擦除素材里的画歪符号
  （自动备份、可重跑；识别"最大橙红簇"，与皮肤/头发天然区分）。

### 测试
- 新增**素材级回归**：pet_rage.png 右上象限橙红像素 < 60
  ——换素材时若星号被 AI 画回来，测试会红并提示重跑擦除工具。
- 渲染测试断言💢的 CustomPaint 存在。
- 累计 **185 项全部通过**。

## [1.3.8] - 2026-09-23

### 修复
- **彩蛋立绘重做**：原「生气」立绘姿势奇怪（抬腿、手举着不像生气），
  且渲染层叠了一条生硬的「上半脸黑色阴影带」，既不像阴影也很难看。
  - **素材**：重新生成 4 张候选（叉腰 / 握拳 / 抱胸等自然站姿 + 皱眉鼓脸 +
    头顶怒气符号），VLM 逐张质检后全部 9 分以上，选用叉腰站姿那张（9.5 分）。
  - **渲染**：**移除**「上半脸阴影带」与降饱和滤镜——情绪应由素材本身表达，
    渲染层只保留背景血色光晕 + 呼吸缩放；立绘尺寸 260 → 292 更突出。

### 工具
- 新增素材质检脚本 `tool/vlm_check_rage.py`：针对「姿势是否自然 / 表情是否到位」
  的结构化评估，避免只看文件大小就判定素材可用。
- 新增抠图体检脚本：
  - `tool/check_cutout_edges.dart`：检查边缘是否残留背景色（黑圈/白边）；
  - `tool/check_on_black.dart`：**黑底合成模拟**——彩蛋是纯黑底，专门验证
    人物外缘不会出现白色光晕（这次实测边缘亮度低于主体，确认安全）。

### 测试
- 累计 **184 项全部通过**（彩蛋渲染测试同步覆盖新结构）。

## [1.3.7] - 2026-09-23

### 修复
- **彩蛋（全屏锁定）界面整屏变灰**（严重）：触发彩蛋时只看到一片灰色，
  台词、血字、桌宠全部不显示。
  - 根因：`ui.ImageFilter.matrix` **要求 4×4 = 16 个元素（列主序）**，
    代码里误写成行主序的 4×5（20 个），运行时抛
    `ArgumentError: "matrix4" must have 16 entries` →
    构建失败 → Flutter 渲染 `ErrorWidget`（即用户看到的灰屏）。
  - 这是 v1.3.3 修「`List<double>` 无法赋给 `Float64List`」时引入的：
    当时补了 `Float64List.fromList` 却把矩阵维度写错了。
  - 修复：改为正确的 4×4 列主序矩阵，并补注释说明排布规则。

### 测试
- 新增 `pet_egg_render_test`（5 项）：模拟 app.dart 的真实层级
  （`Stack → ValueListenableBuilder → PetEggOverlay`），断言彩蛋激活时
  **无异常、无 ErrorWidget**，并验证全屏尺寸、内容文案、多次开关稳定。
  - 该用例在修复前 4 项全红（已实测复现灰屏），修复后全绿。
- 全仓复查了所有 `ImageFilter.*` 调用，无同类参数问题。
- 累计 **184 项全部通过**。

## [1.3.6] - 2026-09-23

### 修复
- **恢复"会跳页"的引导功能，改为正确的跨页引导**（撤销 v1.3.5 的砍功能做法）：
  之前为了让流程"不卡死"，把放大镜、导入等入口从引导里删掉了——这是**用砍功能
  来回避问题**。正确做法是让引导能跨页面进行：
  - 引导流程恢复为完整体验：
    **放大镜 → 搜索页 → 点左上角返回 → 回书架 → 点「＋」→ 最近 → 设置**；
  - 新增 `guideRouteObserver`（路由观察者）：锚点知道自己**被新页面盖住 / 重新露出**，
    被盖住时自动撤销上报，避免高亮框飘在看不见的位置（就是截图里那个现象）；
  - 引导层挂在 `MaterialApp` 之上，跳页后依然可见，因此可以继续指向**新页面上的锚点**
    （搜索页的返回箭头现已注册为 `search_back`）；
  - 弹出面板类入口（如导入面板不占满屏幕）用 `hideWhileOpen`：面板打开期间
    临时收起遮罩，关闭后自动进入下一步，不挡住面板操作。

### 测试
- `pet_guide_test`：新增「流程必须完整覆盖搜书 → 返回 → 导入 → 最近 → 设置」
  与「锚点 id 与页面注册一致」两项，从机制上防止再次砍功能或漏改锚点名。
- `pet_guide_render_test`：新增跨页引导（锚点被覆盖）与面板开合两项渲染测试。
- 累计 **179 项全部通过**。

## [1.3.5] - 2026-09-23

### 修复
- **新手引导流程结构性错误**（两个问题，同一根因）：
  1. 「跳过引导」按钮固定在右上角，**与书架右上角的放大镜重叠**（截图可见高亮框被压住）；
  2. 引导要求用户点击**放大镜**——而放大镜会跳到搜索页，导致后续步骤的锚点
     （书架页的「＋」）随页面一起消失，**引导必然卡死**。
  - 修复 ①：跳过按钮移到**顶部居中**，避开右上角操作区。
  - 修复 ②：重写引导步骤，确立硬约束——
    **只允许指向「常驻导航栏」的锚点**（书架 / 最近 / 设置）；
    会跳页或弹层的入口（放大镜、＋、排列）改为**口头介绍**，不要求点击。
- 引导锚点由「底部导航整条一个锚点」改为**三格各自独立锚点**，
  高亮框现在能准确框住对应那一格。
- 移除书架页上已废弃的危险锚点（`shelf_search` / `shelf_add`）。

### 测试
- `pet_guide_test` 新增 2 项**结构约束测试**：锚点只允许来自白名单、
  禁止使用已知会跳页/弹层的目标（含原因说明），从机制上防止回归。
- `pet_guide_render_test` 新增 2 项布局测试：**跳过按钮不得与锚点重叠**、
  **不得落入右上角操作区**。
- 清理测试中硬编码的锚点 id（改为读取当前步骤真实锚点）。
- 累计 **177 项全部通过**。

## [1.3.4] - 2026-09-23

### 修复
- **新手引导点击后卡死（严重）**：进入「有锚点的步骤」时引导层会直接渲染崩溃，
  表现为遮罩不变暗、点击全部失效、右上角出现灰色错误块，只能重启 App。
  - 根因：把 `Positioned` 包在 `IgnorePointer` 里再放进 `Stack`。
    `Positioned` 是 `ParentDataWidget`，**必须是 `Stack` 的直接子级**，
    否则抛断言并让整棵子树渲染失败。
  - 修复：把 `IgnorePointer` 移到 `Positioned` **内部**
    （`Positioned(child: IgnorePointer(child: ...))`）。
- **补充引导渲染测试**（`pet_guide_render_test`，6 项）：覆盖开场步骤、带锚点步骤、
  整条流程遍历、劝导语气、锚点缺失软化、贴边气泡边界；每个用例都断言
  「无异常抛出 + 不存在 ErrorWidget」。
  - 这些用例在修复前会失败（已验证），确保同类布局错误不会再次漏过。

### 测试
- 累计 **173 项全部通过**（新增 6 项引导渲染测试）。

## [1.3.3] - 2026-09-23

### 修复
- **App 图标补齐 Android 13+ 主题图标（monochrome）**：此前该层仍是 Flutter 模板占位图，
  在支持「主题化图标」的系统上会显示 Flutter 标志；现改为小樱白色人物剪影
  （新增 `gen_icons_test` 的 monochrome 生成，并对像素做纯单色校验）。
- 归档两个图标诊断脚本：`tool/check_icon_sync.py`（APK/res 一致性）、
  `tool/icon_pixel_diff.py`（像素级比对 + 剪影单色性校验）。

### 工程
- 新增 `CHANGELOG.md`、`CONTRIBUTING.md`、`tool/release.sh`、GitHub Actions CI（format/analyze/test/build）。
- `.gitignore` 增补密钥与签名文件规则（`.env*` / `*.jks` / `key.properties` 等）。

## [1.3.2] - 2026-09-23

### 新增
- **新手引导**：首次启动时小樱带新用户走一遍核心入口（自我介绍 → 在线搜书 → 导入本地书 → 设置），
  流程刻意精简；右上角「跳过引导」可随时退出。
  - 不按指引点击时**劝导 5 次**（"……请点一下这里好吗？" → "呵……最后一次机会了哦。"，语气逐次变差）；
  - **第 6 次直接触发全屏锁定彩蛋**；
  - 目标控件用「遮罩留孔 + 高亮环」实现，用户点到的始终是真实按钮；
  - 锚点 1.5 秒内未出现时自动「软化」该步骤（点任意处继续），避免引导卡死。
- **阅读时显示桌宠**：阅读设置面板与软件设置页均新增开关（持久化记忆）。
- **桌宠奔跑乱跑**：小步起伏动画 + 弧线路径 + 朝移动方向前倾；
  主界面低频（25 秒巡检 / 18%）、**阅读时高频捣乱**（14 秒巡检 / 55%）并配专属台词。

### 变更
- **生气系统重构**（体验调优）：
  - 单次拖动生气概率 `0.35 → 0.06`（大幅降低，需反复拖动才可能生气）；
  - 新增「频繁拖动」判定（12 秒内 5 次，概率提到 0.45）；
  - **锁屏门槛从第 2 次提高到第 4 次起**，概率 35% → 55% → 每多一次 +15%（上限 90%）；
  - 触发生气**不再增加亲密度**。
- **彩蛋文案**：底部血字去掉"只有重启才能恢复正常"，仅保留"小樱禁止你使用该软件"。
- **彩蛋视觉强化**：新增血色氛围光、四角暗角、上半脸阴影带、降饱和滤镜。
- **内置书源合规化**：移除两个第三方聚合书源，替换为**中文维基文库**（公版古籍，CC BY-SA）双线路；
  升级时自动清理历史内置的旧源（用户自己导入的同地址源不受影响）。

### 修复
- **在线正文只取到第一段**：`ruleContent` 由 `getString`（仅首段）改为取全部匹配并按段拼接，
  古籍/长章节可完整阅读。
- 书源扩展字段 `webView` / `gatewayPath` / `preRequest` 会错误从 `extra` 中剥离，导致预热请求失效。

### 测试
- 新增 `pet_guide_test`（8 项）、`source_legal_builtin_test`（7 项）、`source_store_legacy_test`（4 项）；
- 重写 `pet_mood_test` 覆盖新门槛与概率；累计 **164 项全部通过**。

## [1.3.1] - 2026-09-22

- **桌宠自由拖动**：去掉自动贴边，拖到哪停在哪；拖动时随机台词。
- **生气系统**：拖动 / 睡觉被叫醒（必定）/ 频繁点击可触发生气表情与专属台词。
- **点击随机蹦跳**：在屏幕内做弧线弹跳（更频繁）。
- **隐藏彩蛋**：全屏黑幕锁定 + 阴沉脸桌宠 + "你以为我是好惹的？"（返回键被拦截）。

## [1.3.0] - 2026-09-21

- **朗读（TTS）**：阅读器底栏新增朗读入口，按段落播报；控制条含进度、上一段/暂停/下一段/设置/退出；
  支持语速与音调调节、朗读自动跟随、读完整章自动连播下一章；无可用引擎时给出安装引导。

## [1.2.10] - 2026-09-21

- **修复桌宠出界**：安全边距 + 弹跳缩放的锚点朝屏幕内 + 台词气泡贴边时向内侧生长。

## [1.2.9] - 2026-09-21

- 桌宠互动反馈升级：点击轻跳、双击摸头飘心、拖拽倾斜摇晃、投喂反应、按时段问候。

## [1.2.8] - 2026-09-21

- 修复桌宠周围一圈黑边（抠图边缘处理）。
- 修复桌宠在开屏动画时提前出现（改为开屏结束 + 权限就绪后显示）。
- 新增打瞌睡系统（深夜 / 长时间未互动自动睡容差分）。

## [1.2.7] - 2026-09-20

- 移除书架左上角的「切换表情包」入口（互动统一收进全局桌宠）。
- **桌宠去背景**：算法抠图（洪泛填充识别背景 + 边缘羽化），6 张看板娘立绘转为透明 PNG。

## [1.2.6] - 2026-09-20

- **全局桌宠**：全 App 悬浮、可拖动、点击互动换表情与台词、长按打开养成面板。
- **亲密度系统**：7 级阶梯（初遇 → 挚爱）；点击 +1 / 摸头 +2 / 投喂 +5~+20 亲密度。
- **点心系统**：只靠阅读时长兑换（樱饼 / 团子 / 大福 / 蛋糕），每天首读额外送 1 个团子。

## [1.2.5] - 2026-09-21

- 书架支持 6 种排序 × 正序/倒序 × 3 种样式（网格 / 小图 / 列表），选择自动记忆。

## [1.2.4] - 2026-09-21

- 阅读界面内的日夜一键切换（阅读背景 + 全局主题同步）。
- 新增书签（收藏位置 / 列表 / 跳回 / 删除）与阅读统计（时长 / 7 天柱状图 / 连续天数）。

## [1.2.1] - 2026-09-20

- 搜索页支持换源；修复图标幂等写入（内容不变不刷新时间戳）。

## [1.0.13] - 2026-09-19

- 修正美术风格（此前误产出写实风素材），重新发包。
